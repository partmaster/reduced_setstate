name: reduced_setstate
description: Implementation of the state management API 'reduced' on top of StatefulWidget and InheritedWidget'.
version: 0.5.0-beta.1
homepage: https://github.com/partmaster/reduced_setstate
repository: https://github.com/partmaster/reduced_setstate.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
#  reduced: 0.5.0
  reduced: 
    git:
      url: https://github.com/partmaster/reduced.git
      ref: v0.5.0-beta.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
